import { SimpleProtectedDirective } from './simple-protected.directive';

describe('SimpleProtectedDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleProtectedDirective();
    expect(directive).toBeTruthy();
  });
});
