import { WriteProtectedDirective } from './write-protected.directive';

describe('WriteProtectedDirective', () => {
  it('should create an instance', () => {
    const directive = new WriteProtectedDirective();
    expect(directive).toBeTruthy();
  });
});
